# Terraform Variables for Combined state rm + import Demo
# Update the bucket_suffix to a unique value to avoid S3 bucket name conflicts

environment   = "demo"
bucket_suffix = "12345"
aws_region    = "us-east-1" 
